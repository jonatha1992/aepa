import React from "react";
import PropTypes from "prop-types";
import "../css/curso.css";



function Card({ title, image, description , autora,precio, duracion, inicio }) {
    console.log(title, image, description);
    return (
        
    );
}

export default Card;
